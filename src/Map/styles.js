import styled from "styled-components"

export const Root = styled.div`
    @media screen and (max-width: 768px) {
        height: 206px;
        width: 100%;
        position: sticky;
        top: 0;
        left: 0;
    }
    position: relative;
    width: calc(70% + 5vw);
`
